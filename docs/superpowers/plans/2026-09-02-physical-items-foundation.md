# Physical Items & Ownership Foundation — TDD Plan

1. Add item value types and `ItemWorld` with canonical location/index invariants.
2. Add character interaction radius, deterministic starting supply stacks, pickup/drop domain primitives, and focused conservation/reach/failure tests.
3. Extend application read model with item revision and explored ground-item snapshots scoped to requested chunks.
4. Render known ground items in Bevy as disposable exact-position markers and cover presentation reconciliation.
5. Update milestone/architecture/README, run fmt/clippy/tests/headless/dependency gate, self-review, merge and push.

[package]
name = "progressus-client"
default-run = "progressus-client"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true

[dependencies]
bevy = { version = "0.18.1", default-features = false, features = [
    "3d_bevy_render",
    "bevy_winit",
    "bevy_audio",
    "wav",
    "default_app",
    "multi_threaded",
    "std",
    "ui_bevy_render",
    "default_font",
    "wayland",
    "x11",
] }
progressus-app = { path = "../progressus-app" }

[[bin]]
name = "progressus-client"
path = "src/main.rs"

[lints]
workspace = true

[package]
name = "progressus-app"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true

[dependencies]
progressus-content = { path = "../progressus-content" }
progressus-sim = { path = "../progressus-sim" }

[lints]
workspace = true
